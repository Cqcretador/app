workflows:
  android-build:
    name: Android Build
    environment:
      flutter: stable
    scripts:
      - flutter clean
      - flutter pub cache repair
      - flutter pub get
      - flutter build apk --debug
    artifacts:
      - build/app/outputs/flutter-apk/app-debug.apk